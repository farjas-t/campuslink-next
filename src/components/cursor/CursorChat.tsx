"use client";
import { CursorChatProps } from "@/types/type";
import React from "react";

const CursorChat = ({
  cursor,
  cursorState,
  setCursorState,
  updateMyPresence,
}: CursorChatProps) => {
  return <div className="">CursorChat</div>;
};

export default CursorChat;
